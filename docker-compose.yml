version: "3.1"

services:
  db:
    image: postgres
    restart: always
    ports:
      - "5431:5431"
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: asp_migration
